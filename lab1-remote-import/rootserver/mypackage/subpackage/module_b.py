"""Модуль во вложенном пакете.

Относительным импортом берёт module_a из родительского пакета —
проверка, что у удалённых пакетов правильно работают __package__ и __path__.
"""

from .. import module_a


def foo():
    """Печатает своё имя и вызывает функцию из родительского пакета."""
    print("module_b from mypackage.subpackage")
    module_a.foo()
